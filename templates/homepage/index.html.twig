{% extends 'base.html.twig' %}

{% block body %}
    <section class="section">
        <h1 class="title has-text-primary">{{ title }}</h1>

        <figure class="image is-3by1">
            <img src="{{ imageUrl }}" />
        </figure>

        <hr />

        {{ render(controller('App\\Controller\\ProductController::featuredProducts')) }}
    </section>
{% endblock %}
